# Environment variables for 3D DBML Viewer
#
# Convention: prefix browser-visible variables with VITE_
# so that Vite exposes them via import.meta.env.VITE_*
#
# Copy this file to .env.local and fill in values as needed.
# Never commit .env.local to source control.
#
# Variables will be added here as new features require them.
